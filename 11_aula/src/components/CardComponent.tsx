import React from "react";

type Props = {};

export default function CardComponent({}: Props) {
	return (
		<div>
			<p>Este é um componente de cartão simples em TypeScript com React.</p>
		</div>
	);
}
